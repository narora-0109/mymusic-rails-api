require 'rails_helper'

RSpec.describe Api::V1::ArtistsController, :type => :controller do
   pending "add some examples to (or delete) #{__FILE__}"

   #   describe "GET #index" do

   #   before do
   #     10.times do
   #      #genre=create(:genre)
   #      artist=create(:artist)
   #      #binding.pry
   #   end

   #   end
   #   it "returns artists with in standard json format that contains entities and links" do
   #     get :index, format: :siren
   #     json = JSON.parse response.body
   #     #binding.pry
   #     expect(json['entities']).to be_present
   #     expect(json['entities']).to be_kind_of(Array)
   #     #expect(json['meta']).to be_present
   #   end
   # end

   #   describe "GET #show" do

   #   before do
   #      artist=create(:artist)
   #   end


   #   it "returns artist with in standard json format that contains entities and links" do
   #     get :show, id: create(:artist).id, format: :siren
   #     json = JSON.parse response.body
   #     #binding.pry
   #     expect(json['properties']).to be_present
   #     expect(json['links']).to be_kind_of(Array)
   #     #expect(json['meta']).to be_present
   #   end
   # end

end
