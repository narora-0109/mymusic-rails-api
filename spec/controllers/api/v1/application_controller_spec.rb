require 'rails_helper'

RSpec.describe Api::V1::ApplicationController, :type => :controller do
  # pending "add some examples to (or delete) #{__FILE__}"

   # describe "GET #index" do

   #   before do
   #     10.times do
   #      genre=create(:genre)
   #      artist=create(:artist,genre_id: genre.id)
   #   end

   #   end
   #   it "returns posts with in standard json format that contains entities and links" do
   #     get action: :index, controller: 'api/v1/artists'
   #     json = JSON.parse response.body
   #     expect(json['entities']).to be_present
   #     expect(json['entities']).to be_kind_of(Array)
   #     #expect(json['meta']).to be_present
   #   end




   # end

end
